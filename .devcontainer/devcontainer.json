{
    "name": "Python 3.12 - Corruption Network Analysis",
    "image": "mcr.microsoft.com/devcontainers/python:3.12-bookworm",
    "features": {
        "ghcr.io/devcontainers/features/git:1": {}
    },
    // Comando unificado garante a ordem correta de execução.
    "postCreateCommand": "sudo apt-get update && sudo apt-get -y upgrade && sudo apt install -y tmux && sudo apt-get update && sudo apt-get install -y libgl1 libegl1 libxcb-cursor0 && pip install --upgrade pip && pip install -r requirements.txt",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "ms-python.vscode-pylance",
                "ms-toolsai.jupyter",
                "eamodio.gitlens",
                "mechatroner.rainbow-csv"
            ]
        }
    }
    // "shutdownAction": "none"
}